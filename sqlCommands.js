// DDL commands
// SELECT email FROM Users WHERE name = 'Patrick Replogle';

// DML commands

// INSERT INTO <table name> (<column names>)
// VALUES (<some values>)

// UPDATE <table name>
// SET <column name> = <some value>
// WHERE <column name> = <some value>

// DELETE FROM <table name>
// WHERE <column name> = <some value>
